/**
 * ItineraryService
 *
 * Generates day-by-day trip itineraries and enforces the no-fly-after-diving
 * safety constraint deterministically.
 */

export type {
  ItineraryService,
  GenerateItineraryParams,
  GenerateItineraryResult,
  ScheduleSurfaceIntervalsParams,
  ScheduleSurfaceIntervalsResult,
} from "./types.js";
