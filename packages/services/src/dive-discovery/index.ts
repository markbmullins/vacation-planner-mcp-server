/**
 * DiveDiscoveryService
 *
 * Handles destination and dive site discovery, seasonality,
 * marine life lookup, and certification fit filtering.
 */

export type {
  DiveDiscoveryService,
  SearchDiveSitesParams,
  SearchDiveSitesResult,
} from "./types.js";
