/**
 * Flight provider adapter interfaces.
 * Concrete provider implementations live here and are injected into services.
 */

export type { FlightProvider, FlightSearchParams, FlightSearchResult } from "./types.js";
