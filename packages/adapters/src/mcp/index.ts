/**
 * MCP server adapter interface.
 * Abstracts the FastMCP framework so it can be swapped without touching
 * service or tool business logic.
 */

export type { McpServerAdapter, McpTool, McpToolContext } from "./types.js";
